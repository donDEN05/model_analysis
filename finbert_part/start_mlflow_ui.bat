@echo off
REM Скрипт для запуска MLflow UI на Windows
setlocal

REM Получаем директорию скрипта
set SCRIPT_DIR=%~dp0
set MLRUNS_DIR=%SCRIPT_DIR%mlruns

REM Проверяем существование директории mlruns
if not exist "%MLRUNS_DIR%" (
    echo ⚠️  Директория mlruns не найдена. Создаю: %MLRUNS_DIR%
    mkdir "%MLRUNS_DIR%"
)

echo ============================================================
echo 🚀 ЗАПУСК MLFLOW UI
echo ============================================================
echo 📁 Директория mlruns: %MLRUNS_DIR%
echo 🌐 MLflow UI будет доступен по адресу: http://localhost:5000
echo ============================================================
echo.

REM Используем Python скрипт для правильного формирования URI
REM Это гарантирует правильный формат пути для Windows
python start_mlflow_ui.py

if errorlevel 1 (
    echo.
    echo ❌ Ошибка при запуске MLflow UI
    echo Убедитесь, что MLflow установлен: pip install mlflow
    pause
    exit /b 1
)

endlocal

